# המרחב המתמטי — math-grids-81

חבילה מלאה לפרסום ב־GitHub Pages.

## העלאה

יש להחליף בריפו את התיקיות `docs` ו־`source` בשלמותן בתיקיות שבחבילה.
בהגדרות GitHub Pages יש להשאיר את הענף `main` ואת התיקייה `/docs`.

## הבסיס לגרסה זו

הגרסה נבנתה ישירות מתוך
`math-grids-81-FINAL-KEYBOARD-PROPERTIES-CSS-FIX`, ללא שימוש בגרסאות הביניים
הקודמות. תיקוני המקלדת וחלונית המאפיינים נשמרו.

## מה נוסף

- כלי „טקסט והערות” זמין בכל שש סביבות העבודה.
- ניתן לכתוב טקסט רב־שורות, לבחור גודל והדגשה ולמקם אותו בכל מקום במישור.
- הטקסט הוא אובייקט שניתן לגרור, לערוך, לצבוע, להסתיר ולמחוק.
- הטקסט נכלל בקובץ PNG שמורידים מהאפליקציה.
- לכל מחוון יש בחירה עצמאית „הצגה על מערכת הצירים”.
- המחוונים המסומנים בלבד מוצגים בתיבה ניידת על המישור.
- ניתן לגרור את תיבת המחוונים ולשנות מתוכה את הערכים בזמן אמת.
- תיבת המחוונים המוצגת נכללת גם בייצוא PNG.
