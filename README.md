# המרחב המתמטי — גרסת המקור עם המדדים לצד המתגים

המאגר: `math-grids-new-81`. יש להחליף את התיקיות `docs` ו־`source` מתוך החבילה.
GitHub Pages: branch `main`, folder `/docs`. הקובץ `docs/.nojekyll` חייב להישאר.

היישומון מבוסס על קובץ המקור שהועלה. נוספו שמות לצירים, הסתרת צירים, כיתוב משוואת הישר וערכי המדדים לצד מתגי התצוגה במאפייני האובייקט.
